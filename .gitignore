# API 키 등 비밀값이 들어있는 실제 설정 파일 (절대 커밋 금지)
config.py

# 파이썬 캐시
__pycache__/
*.pyc

# 로컬 실행 로그 (깃허브 액션에서는 자체 로그를 쓰므로 불필요)
run_log.txt
run_log_old.txt

# 자동 다운로드되는 폰트 파일 (실행 시 자동으로 다시 받아짐)
Anton-Regular.ttf
BlackHanSans-Regular.ttf
